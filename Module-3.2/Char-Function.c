#include<stdio.h>
int main()
{
    /*char no;
    printf("Enter single char:");
    no=getchar();
    putchar(no);
    */
   char nm[20];
   printf("Enter string:");
   gets(nm);
   puts(nm);
    return 0;
}